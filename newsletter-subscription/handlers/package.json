{
  "name": "handlers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:subscription-request-handler": "esbuild ./src/on-newsletter-subscription-requested --bundle --minify --platform=node --target=node16 --outfile=dist/on-newsletter-subscription-requested/index.js",
    "build": "esbuild ./api/* --bundle --minify --platform=node --target=node16 --outdir=public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@devmastery/event-publisher": "workspace:^1.0.0",
    "@vercel/node": "^2.9.10"
  },
  "devDependencies": {
    "esbuild": "^0.17.10",
    "typescript": "^4.9.5"
  }
}
